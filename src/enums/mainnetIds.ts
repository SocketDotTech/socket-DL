import { ChainSlug } from "./chainSlug";

export const MainnetIds: ChainSlug[] = [
  ChainSlug.MAINNET,
  ChainSlug.POLYGON_MAINNET,
  ChainSlug.ARBITRUM,
  ChainSlug.OPTIMISM,
  ChainSlug.BSC,
  ChainSlug.AEVO,
  ChainSlug.LYRA,
  ChainSlug.BASE,
  ChainSlug.MODE,
  ChainSlug.PARALLEL,
];
